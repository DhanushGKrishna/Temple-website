server:
  port: 8082

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/shiva_content?useSSL=false&serverTimezone=UTC
    username: root
    password: Html100@
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
